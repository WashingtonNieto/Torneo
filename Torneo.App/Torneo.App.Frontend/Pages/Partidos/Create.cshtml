@page
@model Torneo.App.Frontend.Pages.Partidos.CreateModel
@{
}
<section class="container">
    <div class="row my-5">
        <div class="col-sm-12 col-md-12 cpñ-lg-12">
            <form method="POST" class="formulario">
                <h3 class="display-5 text-primary text-center">Nuevo partido</h3>
                <div class="form-group">
                    <label asp-for="partido.FechaHora"></label>
                    <input asp-for="partido.FechaHora" class="form-control" placeholder="Fecha y Hora del partido"
                        required>
                    <!-- <span class="text-danger" asp-validation-for="partido.FechaHora"></span> -->
                </div>

                <div class="form-group">
                    <label for="idEquipo">Equipo LOCAL</label>
                    <select id="idEquipo" name="idEquipoLocal" class="form-control">
                        @foreach (var equipo in Model.equipos)
                        {
                            <option value="@equipo.Id">@equipo.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label asp-for="partido.MarcadorLocal"></label>
                    <input asp-for="partido.MarcadorLocal" class="form-control" placeholder="Marcador equipo local" maxlength="20">
                </div>



                <div class="form-group">
                    <label for="idEquipo">Equipo VISITANTE</label>
                    <select id="idEquipo" name="idEquipoVisitante" class="form-control">
                        @foreach (var equipo in Model.equipos)
                        {
                            <option value="@equipo.Id">@equipo.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group">
                    <label asp-for="partido.MarcadorVisitante"></label>
                    <input asp-for="partido.MarcadorVisitante" class="form-control" placeholder="Marcador equipo visitante" maxlength="20">
                </div>


                <button type="submit" class="btn btn-primary btn-block">Crear partido</button>
                <a class="btn btn-secondary" asp-page="./Index">Volver</a>
            </form>
        </div>
    </div>
</section>
